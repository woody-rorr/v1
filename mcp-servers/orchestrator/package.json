{
  "name": "orchestrator",
  "version": "1.0.0",
  "description": "MCP 오케스트레이터 HTTP 서버 — db-schema / business-logic / api-endpoints 자동 실행",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.19.2"
  }
}
